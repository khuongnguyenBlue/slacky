{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "nest start --watch",
      "name": "slacky debugger",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env"
    },
  ]
}
